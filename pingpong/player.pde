class Player {
  String type;
  int score;
  int playerX;
  int playerY;
  
  public Player(String type, int score, int playerX, int playerY) {
    this.type = type;
    this.score = score;
    this.playerX = playerX;
    this.playerY = playerY;
  };
};
